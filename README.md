All features are available through `main.ipynb`. All functionality is available except uploading to Firebase, as the required key is kept private.
### Data
The 25 runs used for training are stored in `\runs`. `\demo_runs` contains the extra verification run as well as 2 runs from the training data used for demonstration.

_Made by group 5 during the Pervasive Computing course at the University of Twente_
